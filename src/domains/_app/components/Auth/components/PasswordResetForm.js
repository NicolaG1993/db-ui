export default function PasswordResetForm({ handleTab }) {
    const [password, setPassword] = useState("");
    const [error, setError] = useState("");

    const handleSubmit = async (e) => {
        e.preventDefault();
        // TODO ... 🔴🔴🔴🚧
    };

    return (
        <div>
            <p>Password Reset</p>
        </div>
    );
}
